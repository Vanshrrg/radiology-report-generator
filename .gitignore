node_modules/
dist/
.DS_Store
*.log

# Raw dictation exports contain real patient names/HN/dates (PHI) — never publish these.
# Cleaned, genericized content lives in src/data/premadeData.js instead.
Template/

# Downloaded word-list sources for scripts/build-dictionary.cjs. The built
# public/dictionary.txt IS committed, so builds never need the network.
scripts/.dict-cache/
